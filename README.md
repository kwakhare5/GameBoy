# GameBoy

This is a code bundle for GameBoy. The original project is available at https://www.figma.com/design/Y5FN3SMV6rLXImieVn0r8k/Clone-Figma-Frame-to-React.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
